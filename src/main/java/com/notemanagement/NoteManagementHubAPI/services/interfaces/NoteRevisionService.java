package com.notemanagement.NoteManagementHubAPI.services.interfaces;

public interface NoteRevisionService {
    /*
    // Manually or automatically save a snapshot
    CompletableFuture<Void> createRevision(UUID noteId, Map<String, Object> contentBody, UUID userId);

    // List all history points for a note
    CompletableFuture<List<RevisionDTO>> getRevisionHistory(UUID noteId, UUID userId);

    // Overwrites current note content with a previous version
    CompletableFuture<NoteResponseDTO> restoreRevision(UUID revisionId, UUID userId);
}
     */
}
